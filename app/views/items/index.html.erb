<p style="color: green"><%= notice %></p>

<h1>Items</h1>

<div id="items">
  <% @items.each do |item| %>
    <%= render item %>
    <p>
      <%= link_to  "Show this item",  group_item_path(item.group_id) %>
    </p>
  <% end %>
</div>

<%= link_to "New item", new_item_path %>
