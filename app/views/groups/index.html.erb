<p style="color: green"><%= notice %></p>

<h1>Groups</h1>

<div class="card p-2" id="groups">
  <% @groups.each do |group| %>
    <%= render group %>
    <p>
      <%= link_to "Show this group", group %>
    </p>
  <% end %>
</div>

<%= link_to "New group", new_group_path %>
